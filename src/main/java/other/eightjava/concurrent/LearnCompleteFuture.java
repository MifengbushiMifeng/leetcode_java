package other.eightjava.concurrent;

import java.util.Random;

/**
 * Execute two tasks( A and B ) in two threads at same time.
 * Once A or B finished, then will execute task C.
 */
public class LearnCompleteFuture {

    private static Random random = new Random();

    public static void main(String[] args) {
    }

}
